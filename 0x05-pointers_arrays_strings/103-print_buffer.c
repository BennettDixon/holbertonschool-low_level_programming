#include <stdio.h>
#include <stdlib.h>
/**
 * print_buffer - prints contents of a buffer
 *
 * @b: buffer to print
 * @size: size of buffer
 *
 * Return: always void
 */
void print_buffer(char *b, int size)
{
	printf("buffer: %s\nsize: %d\n", b, size);
}
