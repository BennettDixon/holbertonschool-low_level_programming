#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char *grammar = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", grammar);
	return (0);
}
